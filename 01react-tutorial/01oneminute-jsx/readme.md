first

```
$ yarn add -D babel-core babel-loader babel-preset-env babel-preset-react-app@3 babel-cli@6
$ yarn add -D webpack webpack-cli
$ yarn add react@16 react-dom@16
$ yarn add -D babel-root-import
```


```
$ yarn install
$ yarn run build
```
